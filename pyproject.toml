[project]
name = "projeto-dnd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Linhares", email = "samuel.linhares@academico.ifpb.edu.br" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
projeto-dnd = "projeto_dnd:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
